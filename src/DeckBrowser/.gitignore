# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

#dist
# ^ for now I decided to commit the bundled index.html file with deck preview tool, so it's ready to use from the .NET flow without the need to locally build the Vite app.

dist-ssr
*.local

# Editor directories and files
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?